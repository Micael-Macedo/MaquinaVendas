@model MAQUINAVENDAS.Models.Category

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Category</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
@if(Model.Items != null){
    <table class="table">
        <tr>
            <th>Item</th>
            <th>Preço</th>
            @foreach (var item in Model?.Items)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.Price</td>    
                </tr>
            }
        </tr>
    </table>
}
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
